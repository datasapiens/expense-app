import { TransactionStoreShape } from '../transactionStore';

// We'll use this file to export all actions associated with the
// transactions slice of the store.
export const { addTransaction } = TransactionStoreShape.actions;
